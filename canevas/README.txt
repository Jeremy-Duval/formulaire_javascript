functions implement :
- draw when you click (with circle)
- change the colors with numbres buttons
- reset the canvas with a button

Problems :
- when you change the color, the last point you have draw change too
- when you reset the canvas, the last point you have draw reappear

Solution :
- delete the last point storage